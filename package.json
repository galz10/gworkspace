{
  "name": "gworkspace",
  "version": "0.1.0",
  "description": "1:1 MCP wrapper CLI for Google Workspace server",
  "type": "module",
  "bin": {
    "gworkspace": "dist/index.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "bun run src/index.ts",
    "start": "node dist/index.js",
    "check": "tsc --noEmit"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.26.0",
    "zod": "^4.1.11"
  },
  "devDependencies": {
    "typescript": "^5.9.3",
    "@types/bun": "^1.3.0",
    "@types/node": "^24.3.0"
  }
}
